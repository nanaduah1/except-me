display_information:
  name: logit
  description: Announces any issues in TDtech production environment
  background_color: "#002182"
features:
  bot_user:
    display_name: logit
    always_online: false
oauth_config:
  scopes:
    bot:
      - incoming-webhook
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
